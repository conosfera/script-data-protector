//codigo para proteger datos en una web
//codigo para un correo electronico
function protector-m()
//by CONOS.io
{
var usuario = "mi-cuenta"
var dominio = "mi-dominio.com"
document.write("<a href=\"mailto:" + usuario + "@" + dominio + "\">" + usuario + "@" + dominio + "</a>")
}
